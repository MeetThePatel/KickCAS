#include <iostream>
#include "../include/ast.h"
#include "../include/parser.h"

int main() {

    /* OPERATOR op = OPERATOR('+'); */
    /* LITERAL x = LITERAL(5); */
    /* LITERAL y = LITERAL(5.3); */
    /* if (x.literalType == LITERAL_TYPE::DOUBLE) { */
    /*     std::cout << "This is a double." << std::endl; */
    /*     std::cout << x.d.value; */
    /* } else if (x.literalType == LITERAL_TYPE::INT) { */
    /*     std::cout << "This is an integer." << std::endl; */
    /*     std::cout << x.i.value << std::endl; */
    /* } */
    /* std::vector<LITERAL> argList{x, y}; */

    /* EXPRESSION test = EXPRESSION(OPERATOR('+'), argList); */
    
}
